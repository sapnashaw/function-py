#9. Explain the purpose and usage of the `map()` function in Python.

#The **map()** function in Python applies a given function to each item of an iterable (like a list) and returns an iterator of the results.


#map(function, iterable)
#Example:
#keypoints:
#numbers = [1, 2, 3]
#squares = map(lambda x: x**2, numbers)
#print(list(squares))  # Output: [1, 4, 9]

#Applies a function to all elements in an iterable.
#Returns a map object (an iterator) which can be converted to a list, tuple, etc.
Useful for concise and efficient data transformations without loops.









